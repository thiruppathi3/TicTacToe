//Automatically generated by rifgen
use crate::*;
use jni_sys::*;
foreign_enum!(
	enum GameState {
		XWon = GameState::XWon,
		OWon = GameState::OWon,
		DRAW = GameState::DRAW,
		Playing = GameState::Playing,
		Error = GameState::Error,
	}
);
foreign_class!(
	class Game {
		self_type Game;
		constructor Game::new()->Game;
		fn Game::input(& mut self , number : i32)->GameState; alias input;
	}
);
